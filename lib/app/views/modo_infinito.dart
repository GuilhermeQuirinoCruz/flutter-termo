import 'package:flutter/material.dart';

class EndlessMode extends StatelessWidget {
  const EndlessMode({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Modo Infinito'),
    );
  }
}